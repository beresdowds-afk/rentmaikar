# Rentmaikar backend environment template
# Generated 2026-09-03 by scanning Deno.env.get() across supabase/functions/** and process.env.* across backend/src/**.
# Values are intentionally empty. Never commit populated copies of this file.
# SUPABASE_* values are injected by the platform and cannot be set manually on Lovable Cloud.

# ── Supabase ──────────────────────────────────────────────
# Runtime-injected function name (16 consumers)
SB_FUNCTION_NAME=
# Publishable client key (52 consumers)
SUPABASE_ANON_KEY=
# Full DB bypass key for server code (155 consumers)
SUPABASE_SERVICE_ROLE_KEY=
# Project API base URL (157 consumers)
SUPABASE_URL=
# Client publishable key (frontend) (2 consumers)
VITE_SUPABASE_PUBLISHABLE_KEY=
# Client build-time URL (frontend) (3 consumers)
VITE_SUPABASE_URL=

# ── Rentmaikar internal ──────────────────────────────────────────────
# Shared token authorising pg_cron -> edge worker calls (52 consumers)
CRON_SECRET=
# Test-only email recipient (1 consumer)
EMAIL_E2E_RECIPIENT=
# Test-only admin token (1 consumer)
TEST_ADMIN_JWT=
# Test fixture user id (1 consumer)
TEST_USER_A_ID=
# Test fixture user id (1 consumer)
TEST_USER_B_ID=

# ── Rentmaikar ──────────────────────────────────────────────
# CORS allowlist for the gateway (1 consumer)
ALLOWED_ORIGINS=
# Legacy alias of PUBLIC_APP_URL (6 consumers)
APP_URL=
# Gateway runtime mode (1 consumer)
NODE_ENV=
# Gateway listen port (1 consumer)
PORT=
# Canonical frontend URL used in links (7 consumers)
PUBLIC_APP_URL=
# Canonical backend/API URL (2 consumers)
PUBLIC_BACKEND_URL=
# Auth redirect base (3 consumers)
SITE_URL=

# ── Sent.dm ──────────────────────────────────────────────
# API base override (staging only) (8 consumers)
SENT_API_BASE_URL=
# Primary global SMS/WhatsApp/RCS API key (x-api-key header) (8 consumers)
SENT_API_KEY=
# Channels Sent may serve (7 consumers)
SENT_CHANNELS=
# Master switch for Sent as primary CPaaS (7 consumers)
SENT_ENABLED=
# Routes traffic to sandbox when true (8 consumers)
SENT_SANDBOX_MODE=
# Approved alphanumeric sender ID (RENTMAIKAR) (8 consumers)
SENT_SENDER_ID=
# Numeric US 10DLC SMS sender (8 consumers)
SENT_SMS_NUMBER=
# Delivery-status callback URL (1 consumer)
SENT_STATUS_WEBHOOK_URL=
# HMAC secret verifying inbound/status callbacks (4 consumers)
SENT_WEBHOOK_SECRET=
# Inbound callback URL (2 consumers)
SENT_WEBHOOK_URL=
# WhatsApp business sender (8 consumers)
SENT_WHATSAPP_NUMBER=

# ── Termii ──────────────────────────────────────────────
# Nigeria SMS/OTP fallback key (12 consumers)
TERMII_API_KEY=
# Approved NG sender ID (8 consumers)
TERMII_SENDER_ID=
# Verifies Termii delivery callbacks (1 consumer)
TERMII_WEBHOOK_SECRET=

# ── Twilio ──────────────────────────────────────────────
# Account identifier (voice only) (27 consumers)
TWILIO_ACCOUNT_SID=
# Alias accepted for API key SID (6 consumers)
TWILIO_API_KEY=
# API key secret (5 consumers)
TWILIO_API_KEY_SECRET=
# API key SID used for REST auth (5 consumers)
TWILIO_API_KEY_SID=
# Alias accepted for API key secret (5 consumers)
TWILIO_API_SECRET=
# Legacy account token (API key preferred) (30 consumers)
TWILIO_AUTH_TOKEN=
# Trust Hub customer profile (1 consumer)
TWILIO_CUSTOMER_PROFILE_SID=
# Legacy from-number alias (1 consumer)
TWILIO_FROM=
# Messaging service (not approved; kept disabled) (2 consumers)
TWILIO_MESSAGING_SERVICE_SID=
# Per-region messaging service override (2 consumers)
TWILIO_MS_NG=
# Per-region messaging service override (2 consumers)
TWILIO_MS_USA=
# Primary US voice number (24 consumers)
TWILIO_PHONE_NUMBER=
# Optional NG long code (1 consumer)
TWILIO_PHONE_NUMBER_NG=
# TwiML app backing the browser softphone (2 consumers)
TWILIO_TWIML_APP_SID=
# Outbound voice caller ID override (3 consumers)
TWILIO_VOICE_FROM=
# Legacy WhatsApp from alias (disabled) (1 consumer)
TWILIO_WHATSAPP_FROM=
# Legacy WhatsApp sender (disabled) (13 consumers)
TWILIO_WHATSAPP_NUMBER=

# ── Resend ──────────────────────────────────────────────
# Outbound transactional email key (25 consumers)
RESEND_API_KEY=
# Fallback sender address (16 consumers)
RESEND_FALLBACK_FROM=
# Verified sending domain (notify.rentmaikar.com) (16 consumers)
RESEND_SENDING_DOMAIN=
# Verifies delivery/bounce/spam webhooks (2 consumers)
RESEND_WEBHOOK_SECRET=

# ── PayPal ──────────────────────────────────────────────
# REST app client ID (11 consumers)
PAYPAL_CLIENT_ID=
# REST app secret (10 consumers)
PAYPAL_CLIENT_SECRET=
# Alias of PAYPAL_MODE (7 consumers)
PAYPAL_ENV=
# sandbox | live (10 consumers)
PAYPAL_MODE=
# Webhook ID used to verify IPN signatures (2 consumers)
PAYPAL_WEBHOOK_ID=

# ── Paystack ──────────────────────────────────────────────
# Publishable checkout key (1 consumer)
PAYSTACK_PUBLIC_KEY=
# NGN charges, transfers and webhook signing (10 consumers)
PAYSTACK_SECRET_KEY=

# ── OPay ──────────────────────────────────────────────
# Alias of OPAY_ENVIRONMENT (5 consumers)
OPAY_ENV=
# sandbox | live (6 consumers)
OPAY_ENVIRONMENT=
# Merchant identifier (6 consumers)
OPAY_MERCHANT_ID=
# Publishable key (6 consumers)
OPAY_PUBLIC_KEY=
# Signs charge and payout requests (8 consumers)
OPAY_SECRET_KEY=

# ── Persona ──────────────────────────────────────────────
# KYC inquiry creation and reconciliation (6 consumers)
PERSONA_API_KEY=
# Environment scope (5 consumers)
PERSONA_ENVIRONMENT_ID=
# Default inquiry template (3 consumers)
PERSONA_MASTER_TEMPLATE_ID=
# Generic template fallback (3 consumers)
PERSONA_TEMPLATE_ID=
# NG template (DB rules override) (1 consumer)
PERSONA_TEMPLATE_ID_NG=
# US template (DB rules override) (1 consumer)
PERSONA_TEMPLATE_ID_US=
# Verifies Persona webhooks (1 consumer)
PERSONA_WEBHOOK_SECRET=

# ── Hologram ──────────────────────────────────────────────
# SIM/cellular management (5 consumers)
HOLOGRAM_API_KEY=
# Organisation scope (5 consumers)
HOLOGRAM_ORG_ID=

# ── Traccar ──────────────────────────────────────────────
# Primary API auth (3 consumers)
TRACCAR_API_TOKEN=
# GPS server base URL (3 consumers)
TRACCAR_BASE_URL=
# Basic-auth fallback user (3 consumers)
TRACCAR_EMAIL=
# Basic-auth fallback password (3 consumers)
TRACCAR_PASSWORD=
# Alias of TRACCAR_API_TOKEN (3 consumers)
TRACCAR_TOKEN=

# ── GPSANDTRACK (Sarekon) ──────────────────────────────────────────────
# API base override (4 consumers)
SAREKON_BASE_URL=
# Telemetry account password (4 consumers)
SAREKON_PASSWORD=
# Alias of SAREKON_USER_ID (4 consumers)
SAREKON_USERNAME=
# Telemetry account user (4 consumers)
SAREKON_USER_ID=

# ── EMQX ──────────────────────────────────────────────
# Management API key (2 consumers)
EMQX_API_KEY=
# Management API secret (2 consumers)
EMQX_API_SECRET=
# MQTT broker management API (2 consumers)
EMQX_API_URL=
# Signs per-vehicle MQTT access tokens (1 consumer)
MQTT_JWT_SECRET=

# ── ElevenLabs ──────────────────────────────────────────────
# Conversational agent identifier (1 consumer)
ELEVENLABS_AGENT_ID=
# Alias accepted by code (6 consumers)
ELEVENLABS_API_KEY=
# TTS/STT and accent conversion (6 consumers)
ELEVEN_LABS_API_KEY=

# ── Lovable AI Gateway ──────────────────────────────────────────────
# Server-side AI completions (23 consumers)
LOVABLE_API_KEY=
# Optional override for the Lovable send endpoint (defaults to https://api.lovable.dev) (1 consumer)
LOVABLE_SEND_URL=

# ── Web Push ──────────────────────────────────────────────
# Signs push payloads (2 consumers)
VAPID_PRIVATE_KEY=
# Public application server key (3 consumers)
VAPID_PUBLIC_KEY=
# mailto: contact for push (2 consumers)
VAPID_SUBJECT=

# ── Firebase ──────────────────────────────────────────────
# Native Android push (1 consumer)
FCM_SERVER_KEY=

# ── Meta ──────────────────────────────────────────────
# Conversions API token (1 consumer)
META_CAPI_ACCESS_TOKEN=
# Conversions API pixel (1 consumer)
META_PIXEL_ID=
# CAPI debugging only (1 consumer)
META_TEST_EVENT_CODE=
# Webhook subscription handshake (1 consumer)
META_WEBHOOK_VERIFY_TOKEN=

# ── WhatsApp Cloud ──────────────────────────────────────────────
# Webhook verification token (1 consumer)
WA_VERIFY_TOKEN=

# ── WhatChimp ──────────────────────────────────────────────
# API base (2 consumers)
WHATCHIMP_API_BASE=
# Optional WhatsApp route (2 consumers)
WHATCHIMP_API_KEY=
# Sender identifier (2 consumers)
WHATCHIMP_PHONE_NUMBER_ID=
# Webhook handshake (1 consumer)
WHATCHIMP_VERIFY_TOKEN=
# Webhook HMAC (2 consumers)
WHATCHIMP_WEBHOOK_SECRET=

# ── ManyChat ──────────────────────────────────────────────
# Optional automation route (2 consumers)
MANYCHAT_API_TOKEN=
# Webhook HMAC (2 consumers)
MANYCHAT_WEBHOOK_SECRET=

